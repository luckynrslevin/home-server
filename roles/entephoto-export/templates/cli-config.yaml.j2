# Rendered by the entephoto-export ansible role.
# Tells the Ente CLI to talk to the self-hosted museum on this host
# instead of the public api.ente.com default.
endpoint:
  api: "{{ entephoto_export_api_endpoint }}"
